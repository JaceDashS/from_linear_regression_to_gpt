Communication job: By the end, learners should understand the conceptual path from simple predictive models to GPT because the deck builds the ideas in sequence.

Source structure: 57-slide, 16:9 white teaching deck with a blue eyebrow, 42 pt black takeaway title, pale slate content panel, and a small bottom-right page marker. The source uses a single Title Slide layout and Master with no inherited visual elements.

Reusable pattern selected: source slide 37. It provides a portrait image frame and a right-hand explanatory sidebar inside the established pale slate content panel. This is the closest source composition for the supplied portrait-oriented AI landscape image.

Insertion contract: duplicate source slide 37, insert it after the roadmap as output slide 2, replace its portrait image, rewrite only its title/sidebar content, add the supplied-image URL to speaker notes, update the roadmap ranges, and renumber visible page markers.

Typography and spacing: preserve the source slide's Calibri family, existing font sizes, colors, panel geometry, margins, and sidebar alignment.

Placeholder audit: the source deck's inspected master/layout contain no inherited elements; the selected slide contains no structural placeholders. The output introduces no empty placeholders.
