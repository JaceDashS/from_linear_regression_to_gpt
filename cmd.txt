:: 1. Windows 파일 경로 길이 제한 해제
reg add "HKLM\SYSTEM\CurrentControlSet\Control\FileSystem" /v LongPathsEnabled /t REG_DWORD /d 1 /f

:: 2. pip 및 기본 패키지 최신화
py -3.12 -m pip install --upgrade pip setuptools wheel

:: 3. llama-cpp-python 및 huggingface_hub 설치
py -3.12 -m pip install llama-cpp-python --extra-index-url https://github.io
py -3.12 -m pip install huggingface_hub

:: 4. 프로젝트 폴더로 이동 및 스크립트 실행
cd C:\docs\from_linear_regression_to_gpt
py -3.12 Qwen2.5_0.5B_Hyperparameter_Lab.py


---

Your entire response must be exactly one of these two strings:

YES
NO

Do not output punctuation, spaces, explanations, greetings, or any other text.
If the user's request cannot be answered using only YES or NO, output NO.